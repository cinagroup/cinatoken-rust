{
  "name": "@cinatoken/drain-source-registration-permit-issuer",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit && vitest run --config vitest.config.ts",
    "test": "vitest run --config vitest.config.ts",
    "types": "wrangler types worker-configuration.d.ts --config wrangler.jsonc --env-interface DrainSourceRegistrationPermitIssuerEnv && wrangler types worker-configuration.staging.d.ts --config wrangler.staging.jsonc --env-interface DrainSourceRegistrationPermitIssuerStagingEnv --include-runtime=false"
  }
}
